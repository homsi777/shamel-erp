import * as sqliteSchema from './schema.sqlite';
import * as pgSchema from './schema.pg';
import { databaseDialect } from './config';

const activeSchema = databaseDialect === 'postgres' ? pgSchema : sqliteSchema;

export const companies = activeSchema.companies;
export const users = activeSchema.users;
export const employees = activeSchema.employees;
export const salaryTransactions = activeSchema.salaryTransactions;
export const biometricDevices = activeSchema.biometricDevices;
export const attendanceRecords = activeSchema.attendanceRecords;
export const parties = activeSchema.parties;
export const partyTransactions = activeSchema.partyTransactions;
export const accounts = activeSchema.accounts;
export const journalEntries = activeSchema.journalEntries;
export const journalEntryLines = activeSchema.journalEntryLines;
export const accountBalances = activeSchema.accountBalances;
export const items = activeSchema.items;
export const textileColors = activeSchema.textileColors;
export const textileStockBalances = activeSchema.textileStockBalances;
export const textileStockMovements = activeSchema.textileStockMovements;
export const itemSerials = activeSchema.itemSerials;
export const itemBarcodes = activeSchema.itemBarcodes;
export const customerItemPrices = activeSchema.customerItemPrices;
export const promotions = activeSchema.promotions;
export const warehouses = activeSchema.warehouses;
export const auditLogs = activeSchema.auditLogs;
export const systemEvents = activeSchema.systemEvents;
export const itemGroups = activeSchema.itemGroups;
export const itemGroupItems = activeSchema.itemGroupItems;
export const agents = activeSchema.agents;
export const agentInventory = activeSchema.agentInventory;
export const agentTransfers = activeSchema.agentTransfers;
export const agentTransferLines = activeSchema.agentTransferLines;
export const agentInventoryMovements = activeSchema.agentInventoryMovements;
export const stockTransfers = activeSchema.stockTransfers;
export const partyTransfers = activeSchema.partyTransfers;
export const deliveryNotices = activeSchema.deliveryNotices;
export const categories = activeSchema.categories;
export const subCategories = activeSchema.subCategories;
export const units = activeSchema.units;
export const cashBoxes = activeSchema.cashBoxes;
export const vouchers = activeSchema.vouchers;
export const invoices = activeSchema.invoices;
export const invoiceMovements = activeSchema.invoiceMovements;
export const systemSettings = activeSchema.systemSettings;
export const queueCounters = activeSchema.queueCounters;
export const warehouseDispatchNotices = activeSchema.warehouseDispatchNotices;
export const warehouseDispatchNoticeLines = activeSchema.warehouseDispatchNoticeLines;
export const warehouseDispatchNoticeLineDecompositions = activeSchema.warehouseDispatchNoticeLineDecompositions;
export const documentSequences = activeSchema.documentSequences;
export const printJobs = activeSchema.printJobs;
export const branches = activeSchema.branches;
export const userBranchAccess = activeSchema.userBranchAccess;
export const userCompanyAccess = activeSchema.userCompanyAccess;
export const reconciliationMarks = activeSchema.reconciliationMarks;
export const remoteBranches = activeSchema.remoteBranches;
export const partners = activeSchema.partners;
export const partnerTransactions = activeSchema.partnerTransactions;
export const recipes = activeSchema.recipes;
export const manufacturingOrders = activeSchema.manufacturingOrders;
export const expenses = activeSchema.expenses;
export const consignmentDocuments = activeSchema.consignmentDocuments;
export const consignmentDocumentLines = activeSchema.consignmentDocumentLines;
export const consignmentSettlements = activeSchema.consignmentSettlements;
export const consignmentSettlementLines = activeSchema.consignmentSettlementLines;
export const consignmentCommissionProfiles = activeSchema.consignmentCommissionProfiles;
export const inventoryMovements = activeSchema.inventoryMovements;
export const fiscalPeriods = activeSchema.fiscalPeriods;
export const printTemplates = activeSchema.printTemplates;
export const printers = activeSchema.printers;
export const reconciliationSessions = activeSchema.reconciliationSessions;
export const reconciliationItems = activeSchema.reconciliationItems;
export const fxRevaluationRuns = activeSchema.fxRevaluationRuns;
export const fxRevaluationLines = activeSchema.fxRevaluationLines;
export const activationCodes = activeSchema.activationCodes;
export const activationTelegramDedupe = activeSchema.activationTelegramDedupe;
export const systemSuperAdmins = activeSchema.systemSuperAdmins;
export const licenseExtensions = activeSchema.licenseExtensions;
export const restaurantTables = activeSchema.restaurantTables;
export const restaurantTableSessions = activeSchema.restaurantTableSessions;
export const restaurantMenuItems = activeSchema.restaurantMenuItems;
export const restaurantTableRequests = activeSchema.restaurantTableRequests;
export const restaurantTableRequestItems = activeSchema.restaurantTableRequestItems;
