
import React from 'react';

// 🛑 EXORCISM COMPLETE 🛑
// This component has been neutralized to prevent the "Blue Djinn" (UI Freezing).
// It no longer listens to events, no longer locks scroll, and renders nothing.
// It remains as a hollow shell to prevent import errors in other files.

const GlobalAlert: React.FC = () => {
  return null;
};

export default GlobalAlert;
